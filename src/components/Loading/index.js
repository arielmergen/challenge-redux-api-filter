import React from 'react';
import { LoadingContainer, LoadingH1 } from './LoadingElements';
const Loading = () => {
    return (
        <LoadingContainer>
            <LoadingH1>Loading SWAPI...</LoadingH1>
        </LoadingContainer>
    );
};

export default Loading;
